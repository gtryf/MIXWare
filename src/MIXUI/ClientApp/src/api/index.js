import User from './User';
import Workspace from './Workspace';
import Submission from './Submission';

export const users = new User();
export const workspaces = new Workspace();
export const submissions = new Submission();